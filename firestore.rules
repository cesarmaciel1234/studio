rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow full read/write for prototypes to ensure list and get operations work for all authenticated users.
      // This solves the "Missing or insufficient permissions" error during development.
      allow read, write: if request.auth != null;
    }
  }
}