rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow full read/write for prototypes.
      // This ensures list and get operations work for all authenticated users.
      allow read, write: if request.auth != null;
    }
  }
}