rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // PERMISOS SÓLIDOS: Permitir acceso de lectura y escritura a cualquier usuario autenticado (incluyendo anónimos)
      allow read, write: if request.auth != null;
    }
  }
}